#include <bits/stdc++.h>

using namespace std;

#define int long long

signed main() {
    double a, b, c;
    cin >> a >> b >> c;
    cout << "MEDIA = " << fixed << setprecision(1) << (a*2.0 + b*3.0 + c*5.0) / 10 << endl;
}