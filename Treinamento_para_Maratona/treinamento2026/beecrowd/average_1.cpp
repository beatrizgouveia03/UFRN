#include <bits/stdc++.h>

using namespace std;

#define int long long

signed main() {
    double a, b, c;
    cin >> a >> b >> c;
    cout << "MEDIA = " << fixed << setprecision(5) << (a*3.5 + b*7.5) / 11 << endl;
}